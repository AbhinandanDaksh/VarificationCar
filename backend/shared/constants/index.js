const { ROLES } = require('./roles');
const { EVENTS } = require('./events');
const {
  VEHICLE_STATUS,
  INSPECTION_STATUS,
  PAYMENT_STATUS,
} = require('./status');

module.exports = {
  ROLES,
  EVENTS,
  VEHICLE_STATUS,
  INSPECTION_STATUS,
  PAYMENT_STATUS,
};
